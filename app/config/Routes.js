/**
 * Define all normal routes, in favor of server-side rendering.
 */
export { default as Home } from '../routes/Home';
export { default as About } from '../routes/About';
export { default as Editor } from '../routes/Editor';
