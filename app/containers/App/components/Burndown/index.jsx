import React from 'react';
import { Heading, Text } from 'spectacle';

const Burndown = () => [
  <Heading key="s3-title">📈 Burndown</Heading>,
  <Text key="s3-placeholder">Lol, placeholder.</Text>,
];

export default Burndown;
